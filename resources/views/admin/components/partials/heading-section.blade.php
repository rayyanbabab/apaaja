<div class="flex flex-col gap-0.5">
    <h1 class="text-xl font-extrabold tracking-tight {{ $titleColor ?? 'text-gray-900' }}">{{ $title }}</h1>
    <p class="text-sm {{ $subtitleColor ?? 'text-gray-400' }}">{{ $subtitle }}</p>
</div>
